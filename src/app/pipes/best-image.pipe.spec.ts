import { BestImagePipe } from './best-image.pipe';

describe('BestImagePipe', () => {
  it('create an instance', () => {
    const pipe = new BestImagePipe();
    expect(pipe).toBeTruthy();
  });
});
